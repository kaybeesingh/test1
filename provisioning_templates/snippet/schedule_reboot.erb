<%#
name: schedule_reboot
snippet: true
model: ProvisioningTemplate
organizations:
- RedHat
locations:
- GSS
-%>
<%#
  Do not use "reboot" command direcly in SSH finish templates, it will break
  SSH connection and cause timeouts. This snippet schedules reboot correctly.
-%>
<% if host_param_true?('schedule_reboot') -%>
PATH=/usr/bin:/usr/sbin:/bin:/sbin:$PATH shutdown -r +1
<% end -%>
